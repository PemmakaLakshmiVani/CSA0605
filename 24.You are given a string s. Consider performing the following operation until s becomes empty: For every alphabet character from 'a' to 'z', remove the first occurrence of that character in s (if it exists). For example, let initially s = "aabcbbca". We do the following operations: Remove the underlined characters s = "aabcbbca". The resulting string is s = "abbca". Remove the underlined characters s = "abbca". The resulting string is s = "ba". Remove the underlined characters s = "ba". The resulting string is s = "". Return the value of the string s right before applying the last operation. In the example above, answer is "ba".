def last_string_before_empty(s):
    for char in 'abcdefghijklmnopqrstuvwxyz':
        s = s.replace(char, '', 1)
        if not s:
            return s
    return s

s = input("Enter a string: ")
print("Last string before empty:", last_string_before_empty(s))
